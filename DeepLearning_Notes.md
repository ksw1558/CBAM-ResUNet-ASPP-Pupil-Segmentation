2026年6月17日最后一次更新。
这个文件是我做这个项目时记录的笔记，边做边学（在学习很多新概念时最好记录下来，在项目中实践，在实践中学习）
# 计算机视觉有四大基本任务:
分类 (Classification)：这张图里有没有瞳孔？（结果：有/没有）
检测 (Detection)：瞳孔在哪里？（结果：画一个方框把瞳孔框住）
语义分割 (Semantic Segmentation)：哪些像素点属于瞳孔？（结果：把属于瞳孔的每一个像素都涂上颜色）
实例分割 (Instance Segmentation)：如果有两只眼睛，哪部分像素属于左眼瞳孔，哪部分属于右眼？
我的任务：U-Net模型正在对图片中的每一个像素点做二分类（0代表背景，1代表瞳孔）。这种“像素级”的分类，是典型的语义分割。

显卡（GPU）是你训练深度学习模型的“动力引擎”。没有它，你的模型也能跑，但速度会慢到让你怀疑人生。

# U-Net模型的理解
U-Net最大的特点就是它的**对称性**，长得像个英文字母U。它由左右两部分组成：
1.左半部分：特征提取网络（Encoder/下采样）
作用：负责“理解”图片。
过程：通过连续的卷积和池化，把一张大图慢慢变小。
通俗理解：就像你在读一本厚书，不断做总结。最后虽然书变薄了（图片尺寸变小），但留下的都是精华特征（比如：这里有个圆，颜色是黑的）。
代价：图片变小的过程中，丢失了精确的位置信息（只知道有瞳孔，但不知道具体在哪一个像素点）。

2.右半部分：特征融合网络（Decoder/上采样）
作用：负责“还原”图片。
过程：通过反卷积（Transpose Convolution），把刚才变小的特征图慢慢放大回原图大小。
通俗理解：根据刚才总结的精华，把瞳孔画回到原来的位置。

卷积：卷积 (Convolution) 是最核心的操作，可以把它理解为模型的“特征提取器”
池化：池化 (Pooling) 是一种下采样操作，主要作用是缩小图片尺寸、提取核心特征。
跳跃连接：把编码器浅层的 “细节图” 直接复制拼接到解码器深层的 “轮廓图” 上，让分割结果更清晰、边缘更准。

# 深度学习基础核心概念
1.关于“圈数”与“批次”
Epoch (轮/圈)：
概念：把整个数据集（那2943张图）全部从头到尾训练一遍，叫做一个Epoch。
通俗理解：相当于你把整本书看了一遍。
Batch Size (批大小)：
概念：模型不是一张图一张图学的，而是一次看一堆图。
通俗理解：你一次翻看几页书。你的设置是8或16，代表显卡一次性吞下8或16张图。
Iteration (步数/迭代)：
概念：每学完一个Batch，就叫做一次迭代。
通俗理解：看一次书（Epoch）需要翻很多次页（Iteration）。

2.关于“学习效率”
Learning Rate(LR/学习率)：
概念：模型参数更新的步长。
通俗理解：你学习的“步子”迈得多大。
注意：步子太大（LR太高）容易扯着蛋，模型会跑飞（Loss变NaN）；步子太小（LR太低）走得太慢，模型老半天没进步。
Cosine Annealing (余弦退火)：
通俗理解：学习率会自动变化。刚开始步子大一点，快点学；快结束时步子小一点，仔细磨，防止错过最优答案。

3.关于“冻结”与“解冻” (Freeze/Unfreeze)
Freeze Training (冻结训练)：
概念：保持主干网络（VGG/ResNet）的权重不变，只训练后面新加的部分。
通俗理解：先用别人的经验（预训练权重）来应付基础考试，自己只练习最后的大题。
好处：省显存，收敛快，不容易把模型带偏。

4.关于“训练结果”
Overfitting (过拟合)：
概念：Total Loss极低，但Val Loss很高。
通俗理解：死记硬背。书上的练习题全对，考试一出新题全废。
Underfitting (欠拟合)
概念：Total Loss 和 Val Loss都很高。
通俗理解：没学进去。还没掌握规律，课后题和考试都不会。

5.关于“损失函数” (Loss Function)
CE (Cross Entropy / 交叉熵)：
作用：对比预测出的每一个像素点，对不对？
Dice Loss：
作用：对比预测出的那块“圆圈”（瞳孔）和真实的圆圈重合度高不高？
意义：你的项目用了这两个组合，能让模型对瞳孔的边缘扣得更准。

6.mIoU(Mean Intersection over Union,平均交并比):
它是语义分割的黄金指标。
计算方式:交集/并集
意义：衡量预测区域与真实区域的重合程度。比准确率更公平，因为它能惩罚那些“只分背景不分瞳孔”的偷懒模型。

7.优化器与**损失函数**:
损失函数(Loss Function)：是衡量*模型预测结果*与*真实标签*之间差距的数学公式。Loss越小，画得越准。
优化器(Optimizer)：它是“改错的方法”。当Loss告诉模型错了，Adam会决定：“我们要把参数往左调一点，还是往右调一点？”
损失函数的作用：
1.指导训练：告诉模型哪里错了
2.反向传播：通过梯度下降更新参数
3.评估性能：Loss越低通常效果越好

常见损失函数：
1.交叉熵损失（CE）
2.Dice Loss
3.Tversky Loss
4.边缘损失（Edge Loss）

loss曲线：
Loss（损失=*模型预测结果*和*真实标签之间*的差距）
模型预测：哪些像素是瞳孔 
真实标签：你标注好的0/1图
两者不一样→就产生loss
Loss曲线=随着训练进行，loss如何变化的图
Loss曲线的作用（重点）：
1. 判断模型有没有学到东西
loss 下降	✅ 模型在学习
loss 不变	❌ 没学到
loss 上升	❌ 学崩了
2. 判断训练是否正常
正常情况：逐渐下降、后期趋于平稳
3. 判断是否过拟合（很重要）

* 1.nets/(Networks-网络架构)
这是整个项目的“大脑”。
作用：存放神经网络的定义代码。
核心文件：
unet.py：定义了U-Net的整体结构（编码器、解码器、跳跃连接）。
vgg.py/resnet.py：存放主干特征提取网络（Backbone）。
白话理解：这里决定了模型“长什么样”，比如有多少层、有没有注意力机制。

* 2.utils/(Utilities-常用工具)
这是项目的“工具箱”或“后勤部”。
作用：存放一些通用的辅助函数，不涉及模型架构，但训练和评估少不了它们。
核心文件：
dataloader.py：负责把磁盘上的图片读进内存，并转成模型能识别的张量（Tensor）。
utils_metrics.py：专门用来计算 mIoU、Precision、Recall 等指标。
callbacks.py：负责在训练过程中自动保存权重、记录 Loss 曲线。
白话理解：这里是处理杂活的地方，比如算分、读数、画图。

* 3.Base_Unet_Exp~CBAM-ResUNet-ASPP
作用：每个文件夹代表一次独立的实验，包含该实验专属的训练脚本、评估脚本和日志记录。
核心文件：
train.py：启动训练的入口，设置学习率、Batch Size、Epoch数等超参数。
eval_xx_voc.py：在VOC数据集上评估模型性能，输出mIoU结果。
logs/：存储每次训练的日志，包括Loss变化图、保存的模型权重（.pth文件）。
miou_out/：存储评估时的预测结果图和计算出的具体指标。
白话理解：这里是实际“跑代码”的地方，每个文件夹记录了一次尝试的过程和结果。

* 4.VOCdevkit/VOC2007/(Dataset-标准数据集)
作用：存放VOC格式的瞳孔分割数据集，用于模型的初步训练和对比。
核心目录：
JPEGImages/：存放原始的眼部图像（输入）。
SegmentationClass/：存放对应的瞳孔标注掩膜（真实答案/Label）。
ImageSets/Segmentation/：存放train.txt和val.txt，规定哪些图用来练，哪些图用来考。

* 5.model_data/(Pretrained Weights-预训练权重)
这是项目的“起跑线”或“经验包”。
作用：存放从ImageNet等大型数据集上预先训练好的权重文件（如vgg16.pth）。
核心价值：迁移学习。让模型不用从零开始学认线条和颜色，而是站在巨人的肩膀上直接学“什么是瞳孔”。
白话理解：相当于给模型请了个有经验的老师，让它上手更快，训练更稳。






